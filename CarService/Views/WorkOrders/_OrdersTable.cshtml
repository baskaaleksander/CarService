@model IEnumerable<ServiceOrderSummaryViewModel>

@if (!Model.Any())
{
    <p class="text-muted">No orders in this category.</p>
}
else
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Order #</th>
                <th>Vehicle</th>
                <th>Status</th>
                <th>Created</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model.OrderByDescending(o => o.CreatedAt))
            {
                <tr>
                    <td><strong>#@order.Id</strong></td>
                    <td>@order.VehicleInfo</td>
                    <td><partial name="_StatusBadge" model="order.Status" /></td>
                    <td>@order.CreatedAt.ToString("d")</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@order.Id" class="btn btn-sm btn-primary">Work on this</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
