@model WorkOrderDetailsViewModel
@{
    ViewData["Title"] = $"Zlecenie #{Model.Id}";
}

<h1>Zlecenie #@Model.Id <partial name="_StatusBadge" model="Model.Status" /></h1>
<hr />

<div class="row">
    <div class="col-md-6">
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-car-front"></i> Informacje o pojeździe</h5>
            </div>
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-4">Pojazd</dt>
                    <dd class="col-sm-8">@Model.VehicleInfo</dd>
                    <dt class="col-sm-4">Klient</dt>
                    <dd class="col-sm-8">@Model.ClientName</dd>
                </dl>
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0"><i class="bi bi-journal-text"></i> Notatki diagnostyczne</h5>
                @if (Model.CanUpdateStatus)
                {
                    <a asp-action="AddNotes" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-secondary">
                        <i class="bi bi-pencil"></i> Edytuj
                    </a>
                }
            </div>
            <div class="card-body">
                @if (!string.IsNullOrEmpty(Model.DiagnosticNotes))
                {
                    <p style="white-space: pre-wrap;" class="mb-0">@Model.DiagnosticNotes</p>
                }
                else
                {
                    <p class="text-muted mb-0">Brak notatek diagnostycznych.</p>
                }
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="mb-0"><i class="bi bi-clock"></i> Godziny pracy</h5>
                @if (Model.CanUpdateStatus)
                {
                    <a asp-action="SetLaborHours" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-secondary">
                        <i class="bi bi-pencil"></i> Aktualizuj
                    </a>
                }
            </div>
            <div class="card-body">
                <p class="fs-4 mb-0"><strong>@Model.LaborHours</strong> godz.</p>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        @if (Model.CanUpdateStatus)
        {
            <div class="card mb-4 border-primary">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="bi bi-lightning"></i> Szybkie akcje</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-action="UpdateStatus" asp-route-id="@Model.Id" class="btn btn-primary">
                            <i class="bi bi-arrow-repeat"></i> Aktualizuj status
                        </a>
                        @if (Model.CanAddItems)
                        {
                            <a asp-action="AddService" asp-route-id="@Model.Id" class="btn btn-outline-success">
                                <i class="bi bi-plus-circle"></i> Dodaj usługę
                            </a>
                            <a asp-action="AddParts" asp-route-id="@Model.Id" class="btn btn-outline-info">
                                <i class="bi bi-box"></i> Dodaj części
                            </a>
                        }
                    </div>
                </div>
            </div>
        }

        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-list-check"></i> Usługi i części</h5>
            </div>
            <div class="card-body">
                @if (Model.Items.Any())
                {
                    <ul class="list-group list-group-flush">
                        @foreach (var item in Model.Items)
                        {
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    @if (item.Type == "Usługa")
                                    {
                                        <span class="badge bg-primary me-2">Usługa</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary me-2">Część</span>
                                    }
                                    @item.Name
                                    @if (item.Quantity > 1)
                                    {
                                        <small class="text-muted">(x@item.Quantity)</small>
                                    }
                                </div>
                                <span>@item.TotalPrice.ToString("C")</span>
                            </li>
                        }
                        <li class="list-group-item d-flex justify-content-between align-items-center bg-light">
                            <strong>Suma</strong>
                            <strong>@Model.TotalCost.ToString("C")</strong>
                        </li>
                    </ul>
                }
                else
                {
                    <p class="text-muted mb-0">Brak dodanych pozycji.</p>
                }
            </div>
        </div>
    </div>
</div>

<partial name="_BackButton" model='"Powrót do zleceń"' />
