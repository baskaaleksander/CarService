@model AddNotesViewModel
@{
    ViewData["Title"] = "Notatki diagnostyczne";
}

<h1>Notatki diagnostyczne</h1>
<p class="text-muted">Zlecenie #@Model.OrderId</p>
<hr />

<partial name="_ValidationSummary" />

<div class="row">
    <div class="col-md-8">
        <form asp-action="AddNotes" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="OrderId" />
            <input type="hidden" asp-for="ExistingNotes" />

            <div class="mb-3">
                <label asp-for="Notes" class="form-label">Notatki</label>
                <textarea asp-for="Notes" class="form-control" rows="10" placeholder="Wpisz ustalenia diagnostyczne, obserwacje i zalecenia..."></textarea>
                <span asp-validation-for="Notes" class="text-danger"></span>
                <div class="form-text">Maksymalnie 2000 znaków.</div>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-check-lg"></i> Zapisz notatki
                </button>
                <a asp-action="Details" asp-route-id="@Model.OrderId" class="btn btn-outline-secondary">Anuluj</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
