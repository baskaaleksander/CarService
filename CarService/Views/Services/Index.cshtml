@model IEnumerable<Service>
@{
    ViewData["Title"] = "Usługi";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1><i class="bi bi-gear"></i> Usługi</h1>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-lg"></i> Dodaj usługę
    </a>
</div>
<hr />

@if (!Model.Any())
{
    <div class="alert alert-info">Brak zarejestrowanych usług.</div>
}
else
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nazwa</th>
                <th>Opis</th>
                <th>Cena</th>
                <th>Status</th>
                <th>Akcje</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var service in Model)
            {
                <tr class="@(!service.IsActive ? "table-secondary" : "")">
                    <td>@service.Name</td>
                    <td><small class="text-muted">@(service.Description?.Length > 50 ? service.Description.Substring(0, 50) + "..." : service.Description)</small></td>
                    <td>@service.Price.ToString("C")</td>
                    <td>
                        @if (service.IsActive)
                        {
                            <span class="badge bg-success">Aktywna</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">Nieaktywna</span>
                        }
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@service.Id" class="btn btn-sm btn-outline-primary">
                            <i class="bi bi-pencil"></i> Edytuj
                        </a>
                        <form asp-action="ToggleActive" asp-route-id="@service.Id" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            @if (service.IsActive)
                            {
                                <button type="submit" class="btn btn-sm btn-outline-danger">
                                    <i class="bi bi-x-circle"></i> Dezaktywuj
                                </button>
                            }
                            else
                            {
                                <button type="submit" class="btn btn-sm btn-outline-success">
                                    <i class="bi bi-check-circle"></i> Aktywuj
                                </button>
                            }
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
