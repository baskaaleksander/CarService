@model ReviewCreateViewModel
@{
    ViewData["Title"] = "Leave a Review";
}

<h1>Leave a Review</h1>
<p class="text-muted">For: @Model.VehicleInfo</p>
<hr />

<partial name="_ValidationSummary" />

<div class="row">
    <div class="col-md-6">
        <form asp-action="AddReview" asp-route-id="@Model.ServiceOrderId" method="post">
            <div class="mb-4">
                <label asp-for="Rating" class="form-label">Your Rating</label>
                <div class="btn-group d-flex" role="group" aria-label="Rating selection">
                    @for (int i = 1; i <= 5; i++)
                    {
                        var starId = $"rating_{i}";
                        <input type="radio" class="btn-check" name="Rating" id="@starId" value="@i" autocomplete="off" @(Model.Rating == i ? "checked" : "")>
                        <label class="btn btn-outline-warning btn-lg flex-fill" for="@starId">
                            @for (int j = 1; j <= i; j++)
                            {
                                <i class="bi bi-star-fill"></i>
                            }
                        </label>
                    }
                </div>
                <span asp-validation-for="Rating" class="text-danger"></span>
            </div>

            <div class="mb-4">
                <label asp-for="Comment" class="form-label">Your Feedback (optional)</label>
                <textarea asp-for="Comment" class="form-control" rows="5" placeholder="Share your experience with us..."></textarea>
                <span asp-validation-for="Comment" class="text-danger"></span>
                <div class="form-text">Tell us what you liked or how we can improve.</div>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-success btn-lg">
                    <i class="bi bi-send"></i> Submit Review
                </button>
                <a asp-action="Details" asp-route-id="@Model.ServiceOrderId" class="btn btn-outline-secondary btn-lg">
                    <i class="bi bi-x-lg"></i> Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
